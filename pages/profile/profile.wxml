<!--pages/profile/profile.wxml-->
<view class="profile-container">
  <!-- 页面标题 -->
  <view class="header">
    <view class="header-title">我的</view>
  </view>

  <!-- 用户信息区域 -->
  <view class="user-section" bindtap="goToProfileInfo">
    <view class="avatar-wrapper">
      <image src="{{userInfo.avatar || '/images/default-avatar.png'}}" class="avatar-img" mode="aspectFill"></image>
    </view>
    <view class="user-details">
      <view class="username">{{userInfo.nickname || '小明爸爸'}}</view>
      <view class="user-meta">
        <text>编号: {{userInfo.id || '10086'}} | 有效期至: {{userInfo.expireDate || '2026-08-07'}}</text>
      </view>
    </view>
    <view class="arrow-icon">
      <l-icon name="right" size="28" color="#CCCCCC"></l-icon>
    </view>
  </view>

  <!-- 功能菜单 -->
  <view class="menu-section">
    <view class="menu-item" bindtap="goToStudyReport">
      <text class="menu-text">学习情况</text>
      <view class="arrow-icon">
        <l-icon name="right" size="28" color="#CCCCCC"></l-icon>
      </view>
    </view>
    <view class="menu-item" bindtap="goToChangePassword">
      <text class="menu-text">修改密码</text>
      <view class="arrow-icon">
        <l-icon name="right" size="28" color="#CCCCCC"></l-icon>
      </view>
    </view>
  </view>

  <!-- 退出登录 -->
  <view class="logout-section">
    <button class="logout-btn" bindtap="handleLogout">退出登录</button>
  </view>
</view>